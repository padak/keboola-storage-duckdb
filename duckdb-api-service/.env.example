# =============================================================================
# DuckDB API Service Configuration
# =============================================================================
#
# Copy this file to .env and adjust values as needed.
# All settings have sensible defaults for local development.
#
# Usage:
#   cp .env.example .env
#   # Edit .env as needed
#   python -m src.main
#

# -----------------------------------------------------------------------------
# Server Settings
# -----------------------------------------------------------------------------

# Server host and port
HOST=0.0.0.0
PORT=8000

# Debug mode - enables /docs (Swagger) and /redoc endpoints
# Set to false in production
DEBUG=true

# -----------------------------------------------------------------------------
# Storage Paths
# -----------------------------------------------------------------------------

# Base data directory - all other paths are derived from this
# For local development, use relative path:
DATA_DIR=./data

# You can override individual paths if needed:
# DUCKDB_DIR=./data/duckdb
# FILES_DIR=./data/files
# SNAPSHOTS_DIR=./data/snapshots
# METADATA_DB_PATH=./data/metadata.duckdb

# -----------------------------------------------------------------------------
# DuckDB Settings
# -----------------------------------------------------------------------------

# Number of threads for DuckDB operations
DUCKDB_THREADS=4

# Memory limit for DuckDB (e.g., "4GB", "8GB", "16GB")
DUCKDB_MEMORY_LIMIT=4GB

# -----------------------------------------------------------------------------
# Timeouts (seconds)
# -----------------------------------------------------------------------------

# Maximum time for a single operation (import, export, query)
OPERATION_TIMEOUT=240

# Connection timeout
CONNECTION_TIMEOUT=10

# -----------------------------------------------------------------------------
# API Settings (usually don't need to change)
# -----------------------------------------------------------------------------

# API_TITLE=DuckDB Storage API
# API_VERSION=0.1.0
# API_PREFIX=/api/v1
